/** Bottom nav on the home screen — drives which panels render. */
export type HomeRoomTab = "lounge" | "bedroom" | "pretzel";

/** LIFX group/location name filter (lowercase match against API names). */
export type LifxRoom = "lounge" | "bedroom";
